#[derive(PartialEq, Clone, Debug)]
pub enum EntryKind {
    AddTag,
    Categorize,
    DeleteConfirmation,
    Find,
    FindName,
    FindLabel,
    Help,
    Information,
    Label,
    MoveConfirmation,
    MoveToLabelConfirmation(String),
    Number,
    Order,
    Rank,
    RemoveTag,
    Rename,
    SetRestriction,
    SetSelection,
    GridSize,
}
