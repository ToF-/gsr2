#[derive(PartialEq, Clone, Debug)]
pub enum EntryKind {
    AddTag,
    Categorize,
    DeleteConfirmation,
    Find,
    FindLabel,
    Help,
    Information,
    Label,
    MoveConfirmation,
    MoveToLabelConfirmation(String),
    Number,
    Order,
    Rank,
    RemoveTag,
    Rename,
    SetRestriction,
    SetSelection,
    GridSize,
}
