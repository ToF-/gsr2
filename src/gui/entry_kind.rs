#[derive(PartialEq, Clone, Debug)]
pub enum EntryKind {
    AddTag,
    Categorize,
    DeleteConfirmation,
    Find,
    FindLabel,
    Help,
    Information,
    Label,
    MoveConfirmation,
    MoveToLabelConfirmation(String),
    Number,
    Order,
    RemoveTag,
    Rename,
    SetRestriction,
    SetSelection,
    GridSize,
}
