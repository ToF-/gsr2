use crate::model::gallery::Gallery;
use crate::gui::navigator::Navigator;
use crate::gui::view::View;
pub fn window_tile(view: &View, navigator: &Navigator, gallery: &Gallery) -> String {
    "foo".to_string()
}
