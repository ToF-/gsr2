use crate::gui::view_state::navigator::Navigator;
use crate::gui::view::View;
use crate::model::gallery::Gallery;

pub fn window_title(view: &View, navigator: &Navigator, gallery: &Gallery) -> String {
    "foo".to_string()
}
